import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: '100%',
    height: 240,
  },
  cardContainer: {flex: 0.5, alignItems: 'center'},
});

export default styles;
